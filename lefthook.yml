pre-commit:
  parallel: true
  commands:
    lint:
      run: task lint
      glob: "**/*.py"
    format-check:
      run: task format-check
      glob: "**/*.py"
    


commit-msg:
  commands:
    commit-msg-check:
      run: uv run commitlint --file {1}


